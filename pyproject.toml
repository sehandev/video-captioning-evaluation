[tool.poetry]
name = "video-captioning-evaluation"
version = "0.1.0"
description = ""
authors = ["sehandev"]
license = "MIT"
readme = "README.md"
packages = [{include = "video_captioning_evaluation"}]

[tool.poetry.dependencies]
python = "^3.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
